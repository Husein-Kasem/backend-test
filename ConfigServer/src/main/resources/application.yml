server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/anouarDahmani/test-tolls/
          search-paths:
          - '{application}'
          #username: username
          #password: password